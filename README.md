# Image-Mail
Render an HTML email to a PNG and emit a new HTML email where every original `&lt;a href>` becomes a clickable region (HTML image map) over its rendered position. The result looks like a flat image to email clients but remains fully clickable.
